class Note {
    constructor({title, note, image, createdDate}) {
        this.title = title;
        this.noteText = note;
        this.image = image;
        this.createdDate = this.createdDate
    }
};

export default Note